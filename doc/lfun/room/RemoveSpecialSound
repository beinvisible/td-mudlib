RemoveSpecialSound()

FUNKTION:
     void RemoveSpecialSound(mixed *keys);

DEFINIERT IN:
     /std/room/description.c

ARGUMENTE:
     keys
          String oder Array von Strings mit den zu entfernenden Sounds.

BESCHREIBUNG:
     Entfernt die in keys angegebenen Sounds aus der Liste der vorhandenen
     SpecialSounds.

RUECKGABEWERT:
     keiner

BEMERKUNGEN:
     Übergibt man für keys eine 0, so werden sämtliche SpecialSounds
     entfernt!

     Da intern Sounds und SpecialSounds im gleichen Mapping verwaltet
     werden, lassen sich mit dieser Funktion auch Sounds entfernen.

SIEHE AUCH:
     AddSpecialSound(), RemoveSound()
     /std/room/description.c

