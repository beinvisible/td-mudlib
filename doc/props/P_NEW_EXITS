NAME:
    P_NEW_EXITS                   "new_exits"                       

DEFINIERT IN:
    /sys/room/exits.h

BESCHREIBUNG:
     Mapping aller unmittelbar sichtbaren Ausgaenge. Die Keys werden
     durch die Namen der Ausgaenge ("norden", etc.) gebildet, die
     Values sind wiederum Mappings, deren Keys in /sys/room/exits.h
     festgelegt sind (EX_DEST, EX_FUNC, etc.) und den Ausgang
     beschreiben.
     Sollte AUSSCHLIESSLICH mit AddExit() und RemoveExit() manipuliert
     werden!!

SIEHE AUCH:
     AddExit, RemoveExit, P_EXITS, P_SPECIAL_EXITS
