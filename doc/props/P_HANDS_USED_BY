NAME:
    P_HANDS_USED_BY               "hands_used_by"               

DEFINIERT IN:
    /sys/living/combat.h

BESCHREIBUNG:
     Enthaelt ein Array von den Objekten, die der Spieler gerade
     in der Hand haelt. Erfordert ein Objekt mehr als eine Hand,
     so kommt es auch entsprechend oft im Array vor.
