RemoveExit()

FUNKTION:
     void RemoveExit(mixed cmd);

DEFINIERT IN:
     /std/room/exits

ARGUMENTE:
     cmd
          String oder Array von Strings mit den Richtungen, die entfernt
          werden sollen.

BESCHREIBUNG:
     Die in cmd angegebenen Ausgaenge werden wieder entfernt.

     Ist cmd = 0, so werden alle Ausgaenge entfernt.

RUECKGABEWERT:
     keiner

BEMERKUNGEN:
     Da normale Ausgaenge und besondere Ausgaenge intern in der gleichen
     Variablen verwaltet werden, entfernt RemoveExit(0) wirklich saemtliche
     Ausgaenge!

SIEHE AUCH:
     AddExit(), RemoveSpecialExit(), GetExits(), /std/room/exits.c

