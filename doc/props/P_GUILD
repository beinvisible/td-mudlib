NAME:
    P_GUILD                       "guild"                       

DEFINIERT IN:
    /sys/properties.h

BESCHREIBUNG:
     Gilde, der der Spieler angehoert.
     Der Name der Gilde ist hier als String definiert (die Gilde
     in Kleinbuchstaben). Bei Spielern ist der Wert dieser Property
     niemals 0 (per Default gehoert ein Spieler der Abenteurergilde an).
