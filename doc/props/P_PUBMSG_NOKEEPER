NAME:
    P_PUBMSG_NOKEEPER             "pubmsg_nokeeper"

DEFINIERT IN:
    /sys/pub.h

BESCHREIBUNG:
    In dieser Property kann man den Text festlegen, der ausgegeben
    wird, wenn der Wirt der Kneipe gerade nicht anwesend ist, aber
    ein Spieler etwas bestellen oder jemandem was spendieren will.
    Ist diese Property nicht definiert, so wird der Text
    QueryProp(P_PUBKEEPER_NAME)+" ist leider nicht da.\n"
    ausgegeben.

SIEHE AUCH:
    P_PUB_FLAGS, P_PUBKEEPER_ID, P_PUBKEEPER_NAME, P_PUBKEEPER_GENDER,
    P_PUBMSG_NOENEMY, P_PUBMSG_NOINVITE, P_PUBMSG_FIGHTING

