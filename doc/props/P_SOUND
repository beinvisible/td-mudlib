NAME:
    P_SOUND                       "sound"

DEFINIERT IN:
    /sys/thing/description.h

BESCHREIBUNG:
    Ein String oder eine Closure (muss einen String zurueckgeben), die
    das "Geraeusch" eines Objekts beschreibt. Dieser Text wird ausgegeben,
    wenn der Spieler "horch an <objekt>" eingibt.

