FUNKTION:
	mixed SetProp( string property, mixed wert );

ARGUMENTE:
	property:	Zeichenkette mit einer Property
	wert:		Beliebiger Wert.

FUNKTION:
	Setze den Wert einer Property auf den angegebenen Wert.
	Der geforderte Typ der Wert haengt stark von der zu setzenden
	Property ab.

RUECKGABEWERT:
	Haengt von der Property ab.

BEMERKUNG:
	Es sollten nur die in properties.h definierten Properties
	benutzt werden.

SIEHE AUCH:
	"/doc/concepts/properties", "/doc/properties.h", "QueryProp"
