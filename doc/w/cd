SYNTAX: cd [ -l ] [ -s ] [ <pfad> ]

ARGUMENTE:
       <pfad> -- neuer Pfad, der gesetzt werden soll

BESCHREIBUNG:
       Setzt den neuen aktuellen Pfad auf pfad oder auf das Heimat Verzeichnis 
       wenn kein Pfad angegeben wurde. 
       Ein Heimat Verzeichnis setzt sich aus /players/Magiername zusammen.
       Wird die Option -l eingestellt, zeigt cd bei Wechsel des aktuellen
       Verzeichnisses die Datei .readme an, soweit diese im neuen Pfad gefunden wird.
       Die Option -s verhindert dies.

BEISPIELE:
       cd /d/inseln
       cd /players/ha*

SIEHE AUCH: prompt, pwd, ls
