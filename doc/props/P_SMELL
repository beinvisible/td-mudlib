NAME:
    P_SMELL                       "smell"

DEFINIERT IN:
    /sys/thing/description.h

BESCHREIBUNG:
    Ein String oder eine Closure (muss einen String zurueckgeben), die
    den Geruch eines Objekts beschreibt. Dieser Text wird ausgegeben,
    wenn der Spieler "riech an <objekt>" eingibt.

