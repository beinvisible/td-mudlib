NAME:
    P_ALIGN_STRING                          "align_string"

DEFINIERT IN:
    /sys/properties.h

BESCHREIBUNG:
    Text der im 'wer' beim Align steht. Ist diese Property auf 0, so wird
    ein Text entsprechende P_ALIGN ausgegeben.
    Bitte NIE P_ALIGN auf einen nicht-integer-Wert setzen, das gibt einen
    Bug im combat.

SIEHE AUCH:
    P_ALIGN
