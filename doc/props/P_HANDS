NAME:
    P_HANDS                       "hands"                       

DEFINIERT IN:
    /sys/living/combat.h

BESCHREIBUNG:
     3-elem. Array:
     1. Elem.: String mit der Meldung, wenn ohne Waffen angegriffen wird.
     2. Elem.: Integer fuer WC, wenn ohne Waffen angegriffen wird.
     3. Elem.: Angriffstyp. Default ist DT_BLUDGEON

BEISPIEL:
     SetProp(P_HANDS, ({" mit seinen Klauen",
                        QueryProp(P_HANDS)[1],
                        DT_RIP}));

SIEHE AUCH:
     man schadenstypen, npc,
     P_HANDS_USED_BY

