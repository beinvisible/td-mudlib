NAME:
    P_CALLED_FROM_IP_NAME         "called_from_ip_name"

DEFINIERT IN:
    /sys/properties.h

BESCHREIBUNG:
     Letzte IP-Adresse, von der aus sich der Spieler eingeloggt hat.
     Hier sollte meist der vom xerq Daemon aufgeloeste Hostname stehen
     (etwa "atpforest.tuwien.ac.at"); konnte der jedoch nicht aufgeloest
     werden, so findet sich hier derselbe Wert wie in P_CALLED_FROM_IP,
     naemlich die numerische IP-Adresse (z.B. "128.130.45.201").
