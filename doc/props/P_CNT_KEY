NAME:
    P_CNT_KEY                  "cnt_key"                  

DEFINIERT IN:
    /sys/container.h

BESCHREIBUNG:
    Bei abschliessbaren behaeltern (dazu muesste /obj/container.c inheriert
    werden) wird diese Property auf die Id des Schluessels gesetzt, der diesen
    Behaelter auf zu schliessen vermag.

SIEHE AUCH:
     /sys/container.h
