NAME:
    P_SHOPKEEPER_ID               "shopkeeper_id"

DEFINIERT IN:
    /sys/laden.h

BESCHREIBUNG: 
    Hier kann man die ID des Haendler festlegen. Sie wird benoetigt,
    wenn in P_SHOP_FLAGS angegeben wurde, dass die Anwesenheit des
    Haendlers Voraussetzung fuer jegliche Spieleraktionen ist.
    Der in std/laden.c definierte Defaultwert ist "haendler".

BEISPIELE:
    siehe P_SHOP_FLAGS

SIEHE AUCH:
    P_SHOP_FLAGS, P_SHOPKEEPER_NAME, P_SHOPKEEPER_GENDER

