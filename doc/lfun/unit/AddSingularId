AddSingularId()

FUNKTION:
     void AddSingularId(string id);

DEFINIERT IN:
     /std/unit.c

ARGUMENTE:
     id
          Identifikationsstring

BESCHREIBUNG:
     Es wird ein Bezeichner hinzugefuegt, mit dem sich eine einzelne Einheit
     ansprechen laesst.

RUECKGABEWERT:
     keiner

BEISPIELE:
     siehe /obj/money.c

SIEHE AUCH:
     AddPluralId(), AddId(), id(), /std/unit.c

