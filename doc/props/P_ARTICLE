NAME:
    P_ARTICLE                     "article"                     

DEFINIERT IN:
    /sys/thing/language.h

BESCHREIBUNG:
     Gibt an, ob in der Beschreibung ein Artikel ausgegeben werden soll
     oder nicht. Handelt es sich bei P_NAME um einen Eigennamen?
     1 ist default.

BEISPIEL:
     SetProp(P_NAME,"Ratte");
     SetProp(P_GENDER, FEMALE);
     SetProp(P_ARTICLE, 1); // default
     
     --> name() ergibt "eine Ratte"
     
     SetProp(P_NAME,"Tivvanie");
     SetProp(P_GENDER, FEMALE);
     SetProp(P_ARTICLE, 0); // Eigenname!
     
     --> name() ergibt "Tivvanie"

SIEHE AUCH:
     P_NAME, name, Name, getuid
