SYNTAX: mv [ -if ] <f1> <f2>
	oder: 
	mv [ -if ] <f1> ... <fn> <d1>

ARGUMENTE:
       <f1>, <f2> -- Dateinamen
       <fn>, <d1> -- Dateiname oder Verzeichnis

BESCHREIBUNG:
       Benennt Datei f1 um nach f2 oder bewegt alle Dateien f1 bis fn in das
       Verzeichnis d1. Ist f2 ein Verzeichnis f1 wird automatisch in das neue
       Verzeichnis f2 bewegt.
       -i -- fragt vor dem Ueberschreiben einer existierenden Datei nach
       -f -- ignoriert alles, auch -i

BEISPIELE:
       mv datei1 datei2
       mv * temp
       mv /players/hate/*.c ../test

SIEHE AUCH: cp, rm
