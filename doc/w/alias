KOMMANDO:

    alias
    ali

BEISPIELE FUER MAGIER:

    ali raumreset xcall ^\$me->reset()

    ali finde xlpc object o; if (!o=find_player("$1"))
              return write("Kein solcher Spieler!\\n");
              write(environment(o)->int_long(o));
        (das alias muss in eine Zeile!)
        "finde <name>" liefert dann, was <name> mit 'schau' sieht

    ali calc xlpc write("$* = "+($*)+"\n");
        (Von Grynnis)
        "calc 8 * 4" liefert dann: 8 * 4 = 32

