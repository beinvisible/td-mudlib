NAME:
    P_MAYSELL                      "maysell"

DEFINIERT IN:
    /sys/properties.h

BESCHREIBUNG:
    Ist diese Property gesetzt, kann das Object in einem Laden auch verkauft
    werden, wenn es P_NODROP gesetzt hat. Allerdings wird dieses dann genauso
    behandelt, als waere P_NOBUY gesetzt!

BEISPIEL:
    SetProp(P_MAYSELL,1);

      - nun sollte man das Objekt trotz gesetzter P_NODROP verkaufen koennen

SIEHE AUCH:
    P_NOBUY, P_NODROP
