RemoveClass()

FUNKTION:
     void RemoveClass(mixed class);

DEFINIERT IN:
     /std/thing/description.c

ARGUMENTE:
     class
          String oder Array von Strings mit den zu entfernenden Klassen.

BESCHREIBUNG:
     Dem Objekt werden einige der mit AddClass() gesetzten Klassen wieder
     entzogen.

RUECKGABEWERT:
     keiner

SIEHE AUCH:
     AddClass(), is_class_member(), /std/thing/description.c

