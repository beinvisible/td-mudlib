NAME:
    P_PUBKEEPER_ID                "pubkeeper_id"

DEFINIERT IN:
    /sys/pub.h

BESCHREIBUNG: 
    Hier kann man die ID des Wirts festlegen. Sie wird benoetigt,
    wenn in P_PUB_FLAGS angegeben wurde, dass die Anwesenheit des
    Wirts Voraussetzung fuer jegliche Spieleraktionen ist.
    Der in std/pub.c definierte Defaultwert ist 0.

BEISPIELE:
    siehe P_PUB_FLAGS

SIEHE AUCH:
    P_PUB_FLAGS, P_PUBKEEPER_NAME, P_PUBKEEPER_GENDER, P_PUBMSG_NOKEEPER,
    P_PUBMSG_NOENEMY, P_PUBMSG_NOINVITE, P_PUBMSG_FIGHTING

