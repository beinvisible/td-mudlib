NAME:
    P_PUBKEEPER_GENDER            "pubkeeper_gender"

DEFINIERT IN:
    /sys/pub.h

BESCHREIBUNG:
    Hier wird das Geschlecht des Wirts festgelegt; diese Angabe
    wird im Moment allerdings nicht wirklich benoetigt und ist
    nur der Vollstaendigkeit halber definiert (analog zu
    P_SHOPKEEPER_GENDER).
    Der in std/pub.c definierte Defaultwert ist 0.

BEISPIELE:
    siehe P_PUB_FLAGS

SIEHE AUCH:
    P_PUB_FLAGS, P_PUBKEEPER_ID, P_PUBKEEPER_NAME, P_PUBMSG_NOKEEPER,
    P_PUBMSG_NOENEMY, P_PUBMSG_NOINVITE, P_PUBMSG_FIGHTING

