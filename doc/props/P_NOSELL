NAME:
    P_NOSELL                      "nosell"

DEFINIERT IN:
    /sys/properties.h

BESCHREIBUNG:
    Diese Property enthaelt eine Meldung, die ausgegeben wird, wenn
    jemand versucht, dieses Objekt zu verkaufen. Wird die Prop. auf einen
    nicht-String-Wert gesetzt, so wird eine Defaultmeldung ausgegeben.

BEISPIEL:
    SetProp(P_NOSELL,1);

      - macht das Objekt unverkaeuflich und gibt eine Defaultmeldung aus

    SetProp(P_NOSELL,break_string(
      "Eigentlich ist Dir xyz so sehr ans Herz gewachsen, dass Du es "+
      "nicht mehr verkaufen willst."));

      - macht das Objekt unverkaeuflich und gibt die angegebene Meldung aus

SIEHE AUCH:
    P_NOBUY, P_MAYSELL, P_NODROP, P_NEVERDROP, P_NOGET
